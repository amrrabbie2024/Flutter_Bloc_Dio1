import 'package:flutter/material.dart';

class RegsisterView extends StatelessWidget {
  const RegsisterView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
